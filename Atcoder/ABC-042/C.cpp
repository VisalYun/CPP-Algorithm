#include <bits/stdc++.h>
using namespace std;

bool checkIndex(vector<int> D, int K){
	
}

int main(){
	int N, K;	
	cin >> N >> K;
	vector<int> D(K);
	for(int i=0; i<K; i++) cin>>D.at(i); 
	
	
	return 0;
}
